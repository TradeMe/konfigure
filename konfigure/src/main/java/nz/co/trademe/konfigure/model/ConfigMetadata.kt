package nz.co.trademe.konfigure.model

/**
 * Base interface for config metadata. This can be extended by consumers using interfaces
 * to attach more information to each config item.
 */
interface ConfigMetadata