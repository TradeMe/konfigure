<?xml version="1.0" encoding="utf-8"?>
<manifest package="nz.co.trademe.konfigure.sample"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".SampleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="nz.co.trademe.konfigure.android.ui.ConfigActivity"
            android:theme="@style/AppTheme.NoActionBar"
            tools:replace="android:theme"/>

        <activity
            android:name=".examples.restart.ConfigRestartActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".examples.filtering.FilteredConfigActivity"
            android:theme="@style/AppTheme.NoActionBar" />

    </application>

</manifest>